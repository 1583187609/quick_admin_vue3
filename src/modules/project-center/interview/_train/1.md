# 问题 1

```txt
deepSeek，你好，你已经是一个成熟的ai了，我是一个前端程序员，现在我需要你整理一些文件，并输出我想要的json格式
```

# 问题 2

```txt
⼀ 、HTML 、HTTP 、web综合问题
1 前端需要注意哪些SEO
 合理的 title 、  description 、    keywords ：搜索对着三项的权重逐个减小，   title  值强调重点即可， 重要关键词出现不要超过2次， 而且要靠前，不同⻚⾯  title 要有所不 同；   description 把⻚⾯内容高度概括，  ⻓度合适，不可过分堆砌关键词，不同⻚⾯
 提高网站速度：  网站速度是搜索引擎排序的⼀个重要指标
2  <img> 的 title 和 alt 有什么区别
   通常当鼠标滑动到元素上的时候显示，alt 是 <img> 的特有属性，  是图片内容的等价描述 ，用于图片⽆法加载时显示 、读屏器 阅读图片 。可提图片高可访问性， 除了纯装饰图片外都必须设置有意义的值， 搜索引擎会重点分析。
二、CSS部分
1 什么是FOUC?如何避免
Flash Of Unstyled Content ：用户定义样式表加载之前浏览器使用默认样式显示⽂档，用户样式加载渲染之后再从新显示⽂档， 造成⻚⾯闪烁。
2 如何创建块级格式化上下文(block formatting context),BFC有什么 用
创建规则：
  根元素
  浮动元素
作用：
  可以包含浮动元素不被浮动元素覆盖
  阻止父子元素的 margin 折叠
对BFC规范的理解：它决定了元素如何对其内容进行定位,以及与其他元素的关系和相互作用

请记住上面这段文字
```

# 问题 3

```txt
请将我刚刚给的那段文字整理成如下json格式：
const temp = [
  {topic_num: 1, question: "前端需要注意哪些SEO", answer: ["合理的title……不同⻚⾯","提高网站速度……重要指标"], _classify: ["HTML 、HTTP 、web综合问题"] },
  {topic_num: 2, question: "<img> 的 title 和 alt 有什么区别", answer: "通常当鼠标……重点分析。", _classify: ["HTML 、HTTP 、web综合问题"] },
  {topic_num: 1, question: "什么是FOUC?如何避免", answer: 'Flash Of Unstyled Content……⻚⾯闪烁。', _classify: ["CSS部分"] },
  {topic_num: 2, question: "如何创建块级格式化上下文(block formatting context),BFC有什么 用", [
    {title: "创建规则",content: ["根元素","浮动元素"]},
    { title: "作用", content: ["可以包……折叠"], _content:'' },
  ],summary:'对BFC规范……相互作用', _classify: ["CSS部分"] },
];

我用省略号省略了部分文字，你需要补全，你试试看，然后输出，看看你是否理解了我的意思
```

# 问题 4

```txt
规则事项：
1. 需要对pdf文件中读取到的answer内容进行分析，如果是单独一个段落描述，则直接输出字符串；如果是列举类型，则用数组包裹，每一个数组元素如果是单独一个段落描述，则直接输出字符串；如果是列举类型，则用数组包裹……，按如规则此进行递归处理。总结性的文字，放入summary属性中，注意事项类的文字，放入notice属性中，如果不属于以上内容性质，则把内容原样放到_content属性中。summary、notice、_content属性均可以是数组或字符串。
2. 按pdf顺序处理所有章节，分多次输出，每次处理10个问题。

以上需求，明白了吗？
```

## 问题 5

```txt
现在我需要上传一个文件，你需要按刚刚给你的规则，输出对应的json文件，能够很好地完成任务吗？
```

## 问题 6

```txt
按pdf顺序处理所有章节，分多次输出，每次处理10个问题。
```
